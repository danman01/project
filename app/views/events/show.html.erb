

<table>
  <thead>
	<th>Name:</th>
 	<th>Date:</th>
	<th>Artist:</th>
	<th>Venue:</th>
	<th>Notes:</th>
	<th>Tickets:</th>
  </thead>
  <tbody>
	<tr>
		<td>
			 <%= @event.name %>
		</td>
		<td><%= @event.date %>
	  	</td>
		<td><%= @event.artist.name %>
	  	</td>
		<td><%= @event.venue.name %>
	  	</td>
		<td>
			<%= @event.notes %>
		</td>
		<td>
			<%=@quantity%>
			<br /><%=link_to 'Add Tickets', {:controller=>'tickets', :action=>'new', :event_id=>@event.id, :artist_id=>@event.artist.id,:venue_id=>@event.venue.id}%>
		</td>
    </tr>
   </tbody>
</table>


<%= link_to 'Edit', edit_event_path(@event) %> |
<%= link_to 'Back', events_path %>
